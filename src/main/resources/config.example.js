// Create config.js in this directory with the following data:
/*
window.iceConfig = {
	iceUrls: ["An ICE server URL that doesn't use authentication, either STUN or TURN."],
	fetchUrls: ["A URL that, when fetch()ed by GET request, responds with a JSON document representing an RTCIceServer object."]
};
*/
